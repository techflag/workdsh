# ADR-0002 仅使用公开运行时及组合机制

状态：已采纳；接口细节由 P0 验证。

使用 npm 发布的 dsh 0.1.5-rc.1、官方 bundle/Profile、Client 扩展和 Agent 执行能力。不获取或修改上游源码。不继承旧 Desktop 仓库双版本约束。

结果：公开能力不足时明确记录，不用私有 import 绕过。专家修订通过原生组合适配；老任务不原地切换专家，使用关联任务交接。
